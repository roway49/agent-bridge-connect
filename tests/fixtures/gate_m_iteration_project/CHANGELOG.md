# Changelog

## 2026-07-04 — Real concurrent AgentBC iteration

- Updated documentation to reflect current project behavior.
- `median` and `range` are now documented as implemented output fields (no longer planned).
- Text utilities `title_case_label`, `extract_keywords`, and `make_slug` documented as implemented.
- Clarified that `--format json|text` CLI flag remains planned.
- Added text utilities reference table to README.
- Added real concurrent AgentBC iteration entry to changelog.

## Unreleased

- Initial fixture for Gate M parallel iteration testing.
- Documented CLI usage, smoke command, and `--format` flag (planned).
- Documented planned output fields: `median`, `range`, `title_case_label`, `extract_keywords`.
- Added parallel-edit ownership notes.
