interface:
  display_name: "AgentBC"
  short_description: "Coordinate AgentBC tasks and reviews"
  default_prompt: "Use $agentbc, read its shared controller contract and steps reference, then inspect or coordinate the exact task ID."

policy:
  allow_implicit_invocation: true
